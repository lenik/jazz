<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.bodz.lily.schema.meta.dao.FormDefMapper">

    <resultMap id="objlist_map" type="net.bodz.lily.schema.meta.FormDef">
        <id property="id" column="id" />
        <result property="code" column="code" />
        <result property="label" column="label" />
        <result property="description" column="description" />
        <result property="priority" column="priority" />
        <result property="flags" column="flags" />
        <result property="state" column="state" />
        <result property="creationDate" column="creation" />
        <result property="lastModifiedDate" column="lastmod" />
        <result property="version" column="version" />
        <result property="subject" column="subject" />
        <result property="rawText" column="text" />
        <result property="schemaId" column="schema" />
        <association property="schema" columnPrefix="schema_"
            javaType="net.bodz.lily.schema.meta.SchemaDef" 
            resultMap="net.bodz.lily.schema.meta.dao.SchemaDefMapper.objlist_map" />
    </resultMap>

    <sql id="objlist_sql"><![CDATA[
        select
            a.id, 
            a.code, 
            a.label, 
            a.description, 
            a.priority, 
            a.flags, 
            a."state", 
            a.creation, 
            a.lastmod, 
            a."version", 
            a."schema", 
            a.subject, 
            a.text, 
            "schema".id schema_id, 
            "schema".code schema_code, 
            "schema".label schema_label, 
            "schema".description schema_description, 
            "schema".priority schema_priority, 
            "schema".flags schema_flags, 
            "schema"."state" schema_state, 
            "schema".creation schema_creation, 
            "schema".lastmod schema_lastmod, 
            "schema"."version" schema_version, 
            "schema".dummy schema_dummy
        from _form a
            left join _schema "schema" on a."schema" = "schema".id
        ]]>
    </sql>

    <sql id="objedit_sql"><![CDATA[
        select
            a.id, 
            a.code, 
            a.label, 
            a.description, 
            a.priority, 
            a.flags, 
            a."state", 
            a.creation, 
            a.lastmod, 
            a."version", 
            a."schema", 
            a.subject, 
            a.text, 
            "schema".id schema_id, 
            "schema".code schema_code, 
            "schema".label schema_label, 
            "schema".description schema_description, 
            "schema".priority schema_priority, 
            "schema".flags schema_flags, 
            "schema"."state" schema_state, 
            "schema".creation schema_creation, 
            "schema".lastmod schema_lastmod, 
            "schema"."version" schema_version, 
            "schema".dummy schema_dummy
        from _form a
            left join _schema "schema" on a."schema" = "schema".id
        ]]>
    </sql>

    <sql id="filtconds">
        <if test="m.id != null">and a.id = #{m.id}</if>
        <if test="m.idRange!= null">
            <if test="m.idRange.hasStartIncl">and a.id >= #{m.idRange.start}</if>
            <if test="m.idRange.hasStartExcl">and a.id > #{m.idRange.start}</if>
            <if test="m.idRange.hasEndIncl">and a.id &lt;= #{m.idRange.end}</if>
            <if test="m.idRange.hasEndExcl">and a.id &lt; #{m.idRange.end}</if>
        </if>
        <if test="m.code != null">and a.code = #{m.code}</if>
        <if test="m.codePattern != null">and a.code like '${m.codePattern}'</if>
        <if test="m.label != null">and a.label = #{m.label}</if>
        <if test="m.labelPattern != null">and a.label like '${m.labelPattern}'</if>
        <if test="m.description != null">and a.description = #{m.description}</if>
        <if test="m.descriptionPattern != null">and a.description like '${m.descriptionPattern}'</if>
        <if test="m.priority != null">and a.priority = #{m.priority}</if>
        <if test="m.priorityRange!= null">
            <if test="m.priorityRange.hasStartIncl">and a.priority >= #{m.priorityRange.start}</if>
            <if test="m.priorityRange.hasStartExcl">and a.priority > #{m.priorityRange.start}</if>
            <if test="m.priorityRange.hasEndIncl">and a.priority &lt;= #{m.priorityRange.end}</if>
            <if test="m.priorityRange.hasEndExcl">and a.priority &lt; #{m.priorityRange.end}</if>
        </if>
        <if test="m.flags != null">and a.flags = #{m.flags}</if>
        <if test="m.flagsRange!= null">
            <if test="m.flagsRange.hasStartIncl">and a.flags >= #{m.flagsRange.start}</if>
            <if test="m.flagsRange.hasStartExcl">and a.flags > #{m.flagsRange.start}</if>
            <if test="m.flagsRange.hasEndIncl">and a.flags &lt;= #{m.flagsRange.end}</if>
            <if test="m.flagsRange.hasEndExcl">and a.flags &lt; #{m.flagsRange.end}</if>
        </if>
        <if test="m.state != null">and a."state" = #{m.state}</if>
        <if test="m.stateRange!= null">
            <if test="m.stateRange.hasStartIncl">and a."state" >= #{m.stateRange.start}</if>
            <if test="m.stateRange.hasStartExcl">and a."state" > #{m.stateRange.start}</if>
            <if test="m.stateRange.hasEndIncl">and a."state" &lt;= #{m.stateRange.end}</if>
            <if test="m.stateRange.hasEndExcl">and a."state" &lt; #{m.stateRange.end}</if>
        </if>
        <if test="m.creationDate != null">and a.creation = #{m.creationDate}</if>
        <if test="m.lastModifiedDate != null">and a.lastmod = #{m.lastModifiedDate}</if>
        <if test="m.version != null">and a."version" = #{m.version}</if>
        <if test="m.versionRange!= null">
            <if test="m.versionRange.hasStartIncl">and a."version" >= #{m.versionRange.start}</if>
            <if test="m.versionRange.hasStartExcl">and a."version" > #{m.versionRange.start}</if>
            <if test="m.versionRange.hasEndIncl">and a."version" &lt;= #{m.versionRange.end}</if>
            <if test="m.versionRange.hasEndExcl">and a."version" &lt; #{m.versionRange.end}</if>
        </if>
        <if test="m.schemaId != null">and a."schema" = #{m.schemaId}</if>
        <if test="m.schemaIdRange!= null">
            <if test="m.schemaIdRange.hasStartIncl">and a."schema" >= #{m.schemaIdRange.start}</if>
            <if test="m.schemaIdRange.hasStartExcl">and a."schema" > #{m.schemaIdRange.start}</if>
            <if test="m.schemaIdRange.hasEndIncl">and a."schema" &lt;= #{m.schemaIdRange.end}</if>
            <if test="m.schemaIdRange.hasEndExcl">and a."schema" &lt; #{m.schemaIdRange.end}</if>
        </if>
        <if test="m.subject != null">and a.subject = #{m.subject}</if>
        <if test="m.subjectPattern != null">and a.subject like '${m.subjectPattern}'</if>
        <if test="m.rawText != null">and a.text = #{m.rawText}</if>
        <if test="m.rawTextPattern != null">and a.text like '${m.rawTextPattern}'</if>
    </sql>

    <select id="all" resultMap="objlist_map">
        <include refid="objlist_sql" />
        <include refid="co.opts" />
    </select>

    <select id="filter" resultMap="objlist_map">
        <include refid="objlist_sql" />
        <where>
            <include refid="filtconds" />
        </where>
        <include refid="co.opts" />
    </select>

    <select id="select" resultMap="objlist_map">
        <include refid="objedit_sql" />
        <where>
            <if test="_parameter != null">a.id = #{id}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"><![CDATA[
        insert into _form(
            code,
            label,
            description,
            priority,
            flags,
            "state",
            creation,
            lastmod,
            "version",
            "schema",
            subject,
            text
        ) values(
            #{code},
            #{label},
            #{description},
            #{priority},
            #{flags},
            #{state},
            #{creationDate},
            #{lastModifiedDate},
            #{version},
            #{schemaId},
            #{subject},
            #{rawText}
        );
    ]]></insert>

    <update id="update">
        update _form
        <set>
            code = #{code},
            label = #{label},
            description = #{description},
            priority = #{priority},
            flags = #{flags},
            "state" = #{state},
            creation = #{creationDate},
            lastmod = #{lastModifiedDate},
            "version" = #{version},
            "schema" = #{schemaId},
            subject = #{subject},
            text = #{rawText},
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <delete id="delete">
        delete from _form
        <where>
            id = #{id}
        </where>
    </delete>

    <select id="count" resultType="long">
        select count(*) "rows" from _form
        <where>
            <if test="_parameter != null">
                <include refid="filtconds" />
            </if>
        </where>
    </select>

    <update id="delete_xrefs">
        alter table lily._formparm drop constraint _formparm_form_fkey;
        alter table lily.article drop constraint article_form_fkey;
        alter table lily.article_msg drop constraint article_msg_form_fkey;
        alter table lily.post drop constraint post_form_fkey;
        alter table lily.post_msg drop constraint post_msg_form_fkey;
        alter table lily.vappreq drop constraint vappreq_form_fkey;
        alter table violet.art_doc drop constraint art_doc_form_fkey;
        alter table violet.diary drop constraint diary_form_fkey;
        alter table violet.diaryrev drop constraint diaryrev_form_fkey;
        alter table violet.fabodr drop constraint fabodr_form_fkey;
        alter table violet.issue drop constraint issue_form_fkey;
        alter table violet.issuel drop constraint issuel_form_fkey;
        alter table violet.plan drop constraint plan_form_fkey;
        alter table violet.plando drop constraint plando_form_fkey;
        alter table violet.saleodr drop constraint saleodr_form_fkey;
        alter table violet.storeodr drop constraint storeodr_form_fkey;
        alter table violet.testq drop constraint testq_form_fkey;
        alter table violet.testq_msg drop constraint testq_msg_form_fkey;
        alter table violet.tranodr drop constraint tranodr_form_fkey;
    </update>

    <update id="create_xrefs">
        alter table lily._formparm
            add constraint _formparm_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete cascade
            ;
        alter table lily.article
            add constraint article_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table lily.article_msg
            add constraint article_msg_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table lily.post
            add constraint post_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table lily.post_msg
            add constraint post_msg_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table lily.vappreq
            add constraint vappreq_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.art_doc
            add constraint art_doc_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.diary
            add constraint diary_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.diaryrev
            add constraint diaryrev_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.fabodr
            add constraint fabodr_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.issue
            add constraint issue_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.issuel
            add constraint issuel_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.plan
            add constraint plan_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.plando
            add constraint plando_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.saleodr
            add constraint saleodr_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.storeodr
            add constraint storeodr_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.testq
            add constraint testq_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.testq_msg
            add constraint testq_msg_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
        alter table violet.tranodr
            add constraint tranodr_form_fkey foreign key (form)
            references lily._form (id)
            on update cascade
            on delete set null
            ;
    </update>

</mapper>
