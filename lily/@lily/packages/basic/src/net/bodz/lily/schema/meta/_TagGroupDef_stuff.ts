import AbstractDefinition from "./AbstractDefinition";

export class _TagGroupDef_stuff<this_t> extends AbstractDefinition<this_t> {

    forTopic: boolean;
    forReply: boolean;

    constructor(o: any) {
        super(o);
    }
}

export default _TagGroupDef_stuff;
