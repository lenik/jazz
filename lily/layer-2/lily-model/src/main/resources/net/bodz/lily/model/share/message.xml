<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="message">

    <resultMap id="mapX" type="com.bee32.zebra.oa.thread.Topic" extends="base.mapUSA">
        <result property="subject" column="subject" />
        <result property="text" column="text" />
        <result property="voteUps" column="nvote" />
        <result property="likerCount" column="nlike" />
        <result property="readCount" column="nread" />
        <association property="op" javaType="net.bodz.lily.model.base.security.User">
            <id property="id" column="op" />
            <result property="label" column="op_label" />
        </association>
    </resultMap>

    <sql id="setX">
        <include refid="base.setUSA" />
        <if test="subject != null">subject=#{subject},</if>
        <if test="text != null">text=#{text},</if>
        <if test="voteUps != null">nvote=#{voteUps - voteDowns},</if>
        <if test="likerCount != null">nlike=#{likerCount},</if>
        <if test="readCount != null">nread=#{readCount},</if>
        <if test="op != null">op=#{op.id},</if>
    </sql>

</mapper>
