<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.bodz.lily.model.base.schema.impl.TagSetDefMapper">

    <resultMap id="map1" type="net.bodz.lily.model.base.schema.TagSetDef" extends="co.mapUS">
        <association property="schema" javaType="net.bodz.lily.model.base.schema.SchemaDef">
            <id property="id" column="schema" />
            <id property="label" column="schema_label" />
        </association>
    </resultMap>

    <sql id="select1"><![CDATA[
        select a.*, s.label schema_label
        from tagv a
            left join schema s on a.schema=s.id
        ]]>
    </sql>

    <sql id="select2"><![CDATA[
        select a.*, s.label schema_label
        from tagv a
            left join schema s on a.schema=s.id
        ]]>
    </sql>

    <select id="all" resultMap="map1">
        <include refid="select1" />
    </select>

    <select id="filter" resultMap="map1">
        <include refid="select1" />
        <where>
            <if test="schemaId != null">and a.schema=#{schemaId}</if>
        </where>
    </select>

    <select id="select" parameterType="int" resultMap="map1">
        <include refid="select2" />
        <where>
            <if test="_parameter != null">a.id=#{id}</if>
        </where>
    </select>

    <select id="insert" resultType="int">
        <![CDATA[
        insert into tagv(code, label, description)
            values(#{codeName},#{label},#{description})
            returning id
    ]]></select>

    <update id="update">
        update tagv
        <set>
            <include refid="co.setUS" />
        </set>
        <where>
            <if test="id != null">id=#{id}, </if>
        </where>
    </update>

    <delete id="delete">
        delete from tagv where id=#{id}
    </delete>

    <select id="count" resultType="hashmap">
        select count(*) "rows" from tagv
    </select>
    
</mapper>
