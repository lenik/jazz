<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.bodz.violet.store.impl.RegionMapper">

    <resultMap id="map1" type="net.bodz.violet.store.Region" extends="co.MAP_UVX">
        <association property="category" javaType="net.bodz.violet.store.RegionCategory">
            <id property="id" column="cat" />
            <result property="label" column="cat_label" />
        </association>
        <association property="parent" javaType="net.bodz.violet.store.Region">
            <id property="id" column="parent" />
            <result property="label" column="parent_label" />
        </association>
    </resultMap>

    <sql id="select1"><![CDATA[
        select
            a.*,
            cat.label "cat_label",
            parent.label "parent_label",
            u.label "uid_label",
            g.label "gid_label"
        from region a
            left join regioncat cat on a.cat = cat.id
            left join region parent on a.parent = parent.id
            left join "user" u on a.uid=u.id
            left join "group" g on a.gid=g.id
        ]]>
    </sql>

    <sql id="select2"><![CDATA[
        select
            a.*,
            cat.label "cat_label",
            parent.label "parent_label",
            u.label "uid_label",
            g.label "gid_label"
        from region a
            left join regioncat cat on a.cat = cat.id
            left join region parent on a.parent = parent.id
            left join "user" u on a.uid=u.id
            left join "group" g on a.gid=g.id
        ]]>
    </sql>

    <sql id="filtconds">

    </sql>

    <select id="all" resultMap="map1">
        <include refid="select1" />
    </select>

    <select id="filter" resultMap="map1">
        <include refid="select1" />
        <where>
            <include refid="filtconds" />
        </where>
    </select>

    <select id="select" parameterType="int" resultMap="map1">
        <include refid="select2" />
        <where>
            <if test="_parameter != null">a.id=#{id}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"><![CDATA[
        insert into region(code, label, description, cat, parent)
            values(#{codeName},#{label},#{description}, #{category.id}, #{parent.id})
            returning id; --
    ]]></insert>

    <update id="update">
        update region
        <set>
            <include refid="co.setU" />
            <if test="category != null">cat=#{category.id}, </if>
            <if test="parent != null">parent=#{parent.id}, </if>
        </set>
        <where>
            <if test="id != null">id=#{id}</if>
        </where>
    </update>

    <delete id="delete">
        delete from region where id=#{id}
    </delete>

    <select id="count" resultType="hashmap">
        select count(*) "total" from region a
        <where>
            <if test="_parameter != null">
                <include refid="filtconds" />
            </if>
        </where>
    </select>
    
</mapper>
