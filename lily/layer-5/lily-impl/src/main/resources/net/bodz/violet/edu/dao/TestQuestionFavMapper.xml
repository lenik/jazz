<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.bodz.violet.edu.dao.TestQuestionFavMapper">

    <resultMap id="objlist_map" type="net.bodz.violet.edu.TestQuestionFav">
        <id property="id" column="id" />
        <result property="testqId" column="testq" />
        <result property="userId" column="user" />
        <association property="testq" columnPrefix="testq_"
            javaType="net.bodz.violet.edu.TestQuestion" 
            resultMap="net.bodz.violet.edu.dao.TestQuestionMapper.objlist_map" />
        <association property="user" columnPrefix="user_"
            javaType="net.bodz.lily.security.User" 
            resultMap="net.bodz.lily.security.dao.UserMapper.objlist_map" />
    </resultMap>

    <sql id="objlist_sql"><![CDATA[
        select
            a.id, 
            a.testq, 
            a."user", 
            testq.id testq_id, 
            testq.uid testq_uid, 
            testq.gid testq_gid, 
            testq.mode testq_mode, 
            testq.acl testq_acl, 
            testq.priority testq_priority, 
            testq.flags testq_flags, 
            testq."state" testq_state, 
            testq.creation testq_creation, 
            testq.lastmod testq_lastmod, 
            testq."version" testq_version, 
            testq.subject testq_subject, 
            testq.op testq_op, 
            testq.text testq_text, 
            testq.form testq_form, 
            testq.formargs testq_formargs, 
            testq.course testq_course, 
            testq.props testq_props, 
            testq.nfav testq_nfav, 
            testq.nvote testq_nvote, 
            testq.nhate testq_nhate, 
            testq.pos testq_pos, 
            testq.answer testq_answer, 
            "user".id user_id, 
            "user"."type" user_type, 
            "user"."name" user_name, 
            "user".label user_label, 
            "user".description user_description, 
            "user".priority user_priority, 
            "user".flags user_flags, 
            "user"."state" user_state, 
            "user".creation user_creation, 
            "user".lastmod user_lastmod, 
            "user"."version" user_version, 
            "user".props user_props, 
            "user".gid0 user_gid0, 
            "user".referer user_referer, 
            "user".person user_person
        from testq_fav a
            left join testq testq on a.testq = testq.id
            left join lily."user" "user" on a."user" = "user".id
        ]]>
    </sql>

    <sql id="objedit_sql"><![CDATA[
        select
            a.id, 
            a.testq, 
            a."user", 
            testq.id testq_id, 
            testq.uid testq_uid, 
            testq.gid testq_gid, 
            testq.mode testq_mode, 
            testq.acl testq_acl, 
            testq.priority testq_priority, 
            testq.flags testq_flags, 
            testq."state" testq_state, 
            testq.creation testq_creation, 
            testq.lastmod testq_lastmod, 
            testq."version" testq_version, 
            testq.subject testq_subject, 
            testq.op testq_op, 
            testq.text testq_text, 
            testq.form testq_form, 
            testq.formargs testq_formargs, 
            testq.course testq_course, 
            testq.props testq_props, 
            testq.nfav testq_nfav, 
            testq.nvote testq_nvote, 
            testq.nhate testq_nhate, 
            testq.pos testq_pos, 
            testq.answer testq_answer, 
            "user".id user_id, 
            "user"."type" user_type, 
            "user"."name" user_name, 
            "user".label user_label, 
            "user".description user_description, 
            "user".priority user_priority, 
            "user".flags user_flags, 
            "user"."state" user_state, 
            "user".creation user_creation, 
            "user".lastmod user_lastmod, 
            "user"."version" user_version, 
            "user".props user_props, 
            "user".gid0 user_gid0, 
            "user".referer user_referer, 
            "user".person user_person
        from testq_fav a
            left join testq testq on a.testq = testq.id
            left join lily."user" "user" on a."user" = "user".id
        ]]>
    </sql>

    <sql id="filtconds">
        <if test="m.id != null">and a.id = #{m.id}</if>
        <if test="m.idRange!= null">
            <if test="m.idRange.hasStartIncl">and a.id >= #{m.idRange.start}</if>
            <if test="m.idRange.hasStartExcl">and a.id > #{m.idRange.start}</if>
            <if test="m.idRange.hasEndIncl">and a.id &lt;= #{m.idRange.end}</if>
            <if test="m.idRange.hasEndExcl">and a.id &lt; #{m.idRange.end}</if>
        </if>
        <if test="m.testqId != null">and a.testq = #{m.testqId}</if>
        <if test="m.testqIdRange!= null">
            <if test="m.testqIdRange.hasStartIncl">and a.testq >= #{m.testqIdRange.start}</if>
            <if test="m.testqIdRange.hasStartExcl">and a.testq > #{m.testqIdRange.start}</if>
            <if test="m.testqIdRange.hasEndIncl">and a.testq &lt;= #{m.testqIdRange.end}</if>
            <if test="m.testqIdRange.hasEndExcl">and a.testq &lt; #{m.testqIdRange.end}</if>
        </if>
        <if test="m.userId != null">and a."user" = #{m.userId}</if>
        <if test="m.userIdRange!= null">
            <if test="m.userIdRange.hasStartIncl">and a."user" >= #{m.userIdRange.start}</if>
            <if test="m.userIdRange.hasStartExcl">and a."user" > #{m.userIdRange.start}</if>
            <if test="m.userIdRange.hasEndIncl">and a."user" &lt;= #{m.userIdRange.end}</if>
            <if test="m.userIdRange.hasEndExcl">and a."user" &lt; #{m.userIdRange.end}</if>
        </if>
    </sql>

    <select id="all" resultMap="objlist_map">
        <include refid="objlist_sql" />
        <include refid="co.opts" />
    </select>

    <select id="filter" resultMap="objlist_map">
        <include refid="objlist_sql" />
        <where>
            <include refid="filtconds" />
        </where>
        <include refid="co.opts" />
    </select>

    <select id="select" resultMap="objlist_map">
        <include refid="objedit_sql" />
        <where>
            <if test="_parameter != null">a.id = #{id}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"><![CDATA[
        insert into testq_fav(
            testq,
            "user"
        ) values(
            #{testqId},
            #{userId}
        );
    ]]></insert>

    <update id="update">
        update testq_fav
        <set>
            testq = #{testqId},
            "user" = #{userId},
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <delete id="delete">
        delete from testq_fav
        <where>
            id = #{id}
        </where>
    </delete>

    <select id="count" resultType="long">
        select count(*) "rows" from testq_fav
        <where>
            <if test="_parameter != null">
                <include refid="filtconds" />
            </if>
        </where>
    </select>

</mapper>
