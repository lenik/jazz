package net.bodz.bas.html.io.tag.gen;

import net.bodz.bas.html.io.HtmlDoc;
import net.bodz.bas.html.io.RecHtmlOut;

/**
 * <!--TOPIC:HTML-->The span element doesn't mean anything on its own, but can be useful when used
 * together with the global attributes, e.g. class, lang, or dir. It represents its children.
 */
public class _HtmlSpan<self_t extends RecHtmlOut<self_t>>
        extends RecHtmlOut<self_t> {

    public _HtmlSpan(HtmlDoc doc) {
        super(doc);
    }

}
