
1. Use shader or assembly?
    shader: allow to merge resources, while assembly has name conflicts.

2. shader won't include indirected referenced dependencies.
    so user have to include those dependencies in their projects.

